define([
	"jquery",
	"use!backbone",
	"use!underscore",
	"use!ui",
	"_s",
	"./chart"
		],
function($, Backbone, _, ui, _s, ChartView){

	var JqPlotChartView = ChartView.extend({

		initialize: function(opts){
		},

		render: function(){
		}

	});

	return JqPlotChartView;
});
		
